pipline {
    agent any
    stages {
        stage('change Directory') {
            steps {
                sh 'cd ~ && cd /var/www'
            }
        }
        stage('Git Checkout') {
            steps {
                sh 'git pull'
            }
        }
    }
}